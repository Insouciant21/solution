#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    double t;
    cin >> t >> n;
    cout << setiosflags(ios::fixed) << setprecision(3) << t / n << endl;
    cout << setprecision(0) << n * 2;
    return 0;
}
