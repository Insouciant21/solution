#include <bits/stdc++.h>

using namespace std;

const int maxn = 60;

int main() {
#ifdef LOCALENV
    freopen("test.in", "r", stdin);
    freopen("test.out", "w", stdout);
#endif
    int f[maxn][maxn][6];

    return 0;
}